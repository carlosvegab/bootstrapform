<div class="mb-3">
    <label for="{{ $fieldname }}" class="form-label">{{ $labelname }}</label>
    <textarea class="form-control" name="{{ $fieldname }}" id="{{ $fieldname }}" rows="3">{{ $value }}</textarea>
</div>
