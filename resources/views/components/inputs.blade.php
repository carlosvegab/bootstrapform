<div class="mb-3">
    <label for="{{ $fieldname }}" class="form-label">{{ $labelname }}</label>
    <input type="{{ $type }}" class="form-control" name="{{ $fieldname }}" id="{{ $fieldname }}" placeholder="{{ $fieldname }}" value="{{ $value }}">
</div>
